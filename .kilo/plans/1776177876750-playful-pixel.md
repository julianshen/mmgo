# Execute Flowchart Renderer Implementation Plan

Execute the plan at `docs/superpowers/plans/2026-04-15-flowchart-renderer.md` to build `pkg/renderer/flowchart/`.

## Context

- **Spec:** `docs/superpowers/specs/2026-04-14-flowchart-renderer-design.md`
- **Branch:** Create `feature/renderer-flowchart` from `main`
- **Steps:** 8 tasks across 4 chunks

## Execution Steps

1. Create feature branch `feature/renderer-flowchart`
2. Execute Chunk 1 (Tasks 1-3): SVG structs, Theme/Options, Render stubs
3. Execute Chunk 2 (Tasks 4): All 14 node shapes + tests
4. Execute Chunk 3 (Tasks 5): Edge rendering with markers, curves, labels + tests
5. Execute Chunk 4 (Tasks 6-8): Subgraphs, style/class, golden files, coverage
6. Run `go test ./... -race -cover` and `golangci-lint run ./pkg/renderer/flowchart/...`
7. Update `docs/STATUS.md`
8. Create PR

## Key Notes

- Go 1.22 builtins `min`/`max` — no manual implementations
- All tasks must produce compilable state (stubs first)
- Use `strings.Contains` — no custom helpers
- Marker matching by `diagram.ArrowHead` type, not substring search
- Golden test uses aliased `fcparser` import for parser
